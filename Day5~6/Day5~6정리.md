# 📅 Day 5-6: 로직의 재사용과 캡슐화 (Functions)

## 🔍 심화 학습 (Deep Dive)
### 1. 함수의 반환(Return) 법칙
- 모든 함수는 무언가를 반환한다. 명시적으로 `return`을 쓰지 않으면 자바스크립트는 자동으로 `undefined`를 반환한다.
- 화살표 함수에서 `{}`를 쓰면 `return`을 명시해야 하고, `{}`를 생략하면 그 줄의 결과가 자동으로 반환된다.

### 2. 매개변수(Parameter) vs 인자(Argument)
- **Parameter**: 함수를 정의할 때 사용하는 변수 이름 (예: a, b).
- **Argument**: 함수를 실제로 호출할 때 넣는 실제 값 (예: 10, 5).

## 🛠 실무 코드 패턴 (Code Patterns)
```javascript
// 데이터 포맷팅 전문 함수
const formatCurrency = (amount) => {
  return new Intl.NumberFormat('ko-KR', { style: 'currency', currency: 'KRW' }).format(amount);
};
console.log(formatCurrency(50000)); // ₩50,000